{

    "name": "selenium-mocha-digital-skola",

    "version": "1.0.0",

    "description": "Selenium WebDriver tests using Mocha with Mochawesome reports",

    "scripts": {

        "test": "mocha test/Test-1.js --recursive --timeout 60000",
        "test2": "mocha Test/Specs --recursive --timeout 60000",
        "reportmochawesome": "mocha test --recursive --timeout 60000 --reporter mochawesome",
        "reportsimple-html": "mocha test --recursive --timeout 60000 --reporter mocha-simple-html-reporter --reporter-options output=report.html"
    },

    "dependencies": {

        "selenium-webdriver": "^4.17.0",
        "pixelmatch": "^7.1.0",
        "pngjs": "^7.0.0"

    },

    "devDependencies": {

        "mocha": "^10.4.0",

        "mocha-simple-html-reporter": "^2.0.0",

        "mochawesome": "^7.1.3"

    }

}